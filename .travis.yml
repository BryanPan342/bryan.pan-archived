language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
scripts:
  - yarn build
deploy:
  provider: pages
  github-token: $GITHUB_TOKEN  
  committer-from-gh: true
  skip-cleanup: true
  keep-history: true
  local-dir: build
  target-branch: gh-pages
  on:
    branch: master